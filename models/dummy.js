var saveModel = require('../helpers/modelsaver.js');

/*var dummySchema = mongoose.Schema({
  name: String
});
var DummyModel = mongoose.model('dummies', dummySchema);

var newDummy1 = new DummyModel({name: "PIETJE"});
var newDummy2 = new DummyModel({name: "JANTJE"});
var newDummy3 = new DummyModel({name: "KLAASJE"});

var dummieModels = [newDummy1, newDummy2, newDummy3];

dummieModels.forEach(function(model){
  model.save(saveModel);
});*/